[package]
name = "ib_breakout_strategy"
version = "0.1.0"
edition = "2021"
description = "Initial Balance Breakout Strategy Backtest"

[dependencies]
csv = "1.3"
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
