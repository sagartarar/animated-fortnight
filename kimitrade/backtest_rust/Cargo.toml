[package]
name = "kimitrade_backtest"
version = "0.1.0"
edition = "2021"

[dependencies]
csv = "1.3"
serde = { version = "1.0", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
rayon = "1.8"
glob = "0.3"
indicatif = "0.17"

[profile.release]
opt-level = 3
lto = true
